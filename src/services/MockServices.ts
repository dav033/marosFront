// Deprecated mock services placeholder. Keep file to avoid broken imports.
// If you need mocks, implement here or remove imports where used.

export const MockServices = {
  // Example:
  // getSomething: async () => Promise.resolve([]),
};
