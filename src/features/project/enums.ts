export enum ProjectStatus {
  NOT_EXECUTED = "NOT_EXECUTED",
  IN_PROGRESS = "IN_PROGRESS",
  COMPLETED = "COMPLETED",
  LOST = "LOST",
  POSTPONED = "POSTPONED",
}

export enum InvoiceStatus {
  PAID = "PAID",
  PENDING = "PENDING",
  NOT_EXECUTED = "NOT_EXECUTED",
}
